server {
    listen 80;
    server_name 194.87.191.45;

    location = /favicon.ico { access_log off; log_not_found off; }
    location / {
        root /root/new_horizon_project;
        include proxy_params;
        proxy_pass http://unix:/run/new_horizon_gunicorn.sock;
    }
}
