from django.apps import AppConfig


class AccountPageConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.account_page"

    verbose_name = "1. Личный кабинет пользователя"
